# 如何修改主页

## 修改文字

用记事本或 VS Code 打开 `src/data.js`。主页的主要文字都集中在这个文件中：

- `profile`：姓名、学校、邮箱、所在地、更新时间和侧栏链接
- `about`：首页大标题、个人简介和研究方向
- `updates`：What's New 时间线
- `education`：教育经历和课程
- `research`：研究经历
- `publications`：论文
- `awards`：奖项
- `more`：语言、技能、学生工作和兴趣

只修改单引号中的文字即可。英文单词中的单引号需要写成 `\'`，并注意不要删除逗号、引号和括号。

其中 `profile.cvFilename` 控制访客下载 CV 时看到的文件名。

## 本地实时预览

双击 `live-preview.cmd`，浏览器会自动打开本地预览页面。保持命令行窗口开启，然后保存 `src/data.js`，浏览器会立即同步更新，不需要反复生成 HTML。

实时预览确认无误后，再双击 `rebuild-homepage.cmd` 生成最终的 `homepage.html`。

## 修改照片

用新照片替换 `images/profile.jpg`，文件名必须继续保持为 `profile.jpg`。网页会自动按照图片的原始宽高比例完整显示，不进行强制裁切。

## 修改 CV

同时替换以下两个文件，并保留原文件名：

- `public/Jinzhao_Lian_CV.pdf`
- `src/assets/Jinzhao_Lian_CV.pdf`

## 重新生成主页

修改完成后，双击项目根目录中的 `rebuild-homepage.cmd`。看到 `Done` 后，再双击 `index.html` 或 `homepage.html` 查看结果。

## 在 GitHub 上修改

将整个 `jinzhao-homepage` 项目上传到 GitHub，而不是只上传 `homepage.html`。仓库已包含 GitHub Pages 自动部署配置。

在 GitHub 网页中修改 `src/data.js` 并提交到 `main` 分支后，GitHub Actions 会自动重新构建和发布主页。照片在 `images/profile.jpg`，下载版 CV 的构建源文件在 `src/assets/Jinzhao_Lian_CV.pdf`。

第一次部署时，在 GitHub 仓库中进入 `Settings → Pages`，将 `Source` 设置为 `GitHub Actions`。
